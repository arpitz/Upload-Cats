import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
    thumbUp: {
    color : "green",
    cursor: "pointer"
  },
  thumbDown: {
    color : "red",
    cursor: "pointer"
  },
  score: {
    marginLeft: "10px",
    fontWeight: "bold"
  }
});
